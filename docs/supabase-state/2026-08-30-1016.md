# Supabase state — 2026-08-30 10:16 Europe/Paris

## Scope

Read-only diagnostic snapshot for RailOps project `tbmzmmamaiftbbbuelgd` while working only on Git branch `security/v150b2b-rls-ready`.

No SQL, migration, RLS policy, permission, schema, data, Auth setting, Edge Function, deployment, merge, or production change was performed.

## Project health

- Project: `railops`
- Region: `eu-north-1`
- Database: PostgreSQL `17.6.1.084`
- Supabase status: `ACTIVE_HEALTHY`

## Security advisor baseline observed

The read-only security advisor currently reports these known categories:

- `rls_enabled_no_policy` on `public.agents`, `public.inspections`, `public.materiel`, `public.railops_auth_throttle`, and `public.railops_legacy_credentials`.
- `authenticated_security_definer_function_executable` on multiple existing `public.railops_*` RPC functions, including administrative, catalogue, chantier, material, scan, session and profile helpers.
- `auth_leaked_password_protection`: leaked-password protection is disabled.

These findings are recorded for comparison only. They are not treated as safe autonomous changes because altering them can change permissions, security semantics, authentication behaviour, or application access paths.

## Decision boundary

Do not enable strict RLS, add/revoke policies or grants, change `SECURITY DEFINER` semantics, change Auth password settings, or otherwise remediate these findings without explicit Yohan approval and a dedicated validation plan.

## Verification

- Git branch head before this snapshot: `8068778fd1b978455c27e48732bc33320e2b29cb`.
- `main` observed and left untouched at `37b216936a6692d54f82cbc004b30c936d13785a`.
- Supabase was inspected read-only; project remained `ACTIVE_HEALTHY`.
