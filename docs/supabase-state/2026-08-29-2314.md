# RailOps — état GitHub / Supabase — 2026-08-29 23:14 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail observée avant ce snapshot : `security/v150b2b-rls-ready` au commit `7019480e386a16c8519570f10385e9b73be7c22b`.
- `main` observé et laissé intact : `37b216936a6692d54f82cbc004b30c936d13785a`.
- Comparaison fraîche `main...security/v150b2b-rls-ready` : **0 commits behind / 631 ahead**, statut `ahead`; merge-base `37b216936a6692d54f82cbc004b30c936d13785a`.
- Garde-fou Git : aucun merge, rebase, cherry-pick ou changement de `main` n'est effectué par ce relevé.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État observé : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`, région `eu-north-1`.
- Volumétrie cœur observée : `scans=124`, `materiels=1689`, `users=34` ; valeurs stables par rapport au relevé de 22:18.

### Security Advisor

Le baseline reste matériellement inchangé : tables avec RLS activé sans policy, fonctions `SECURITY DEFINER` exécutables par `authenticated`, et protection contre les mots de passe compromis désactivée. Ces éléments restent hors périmètre automatique et ne sont pas modifiés.

### Performance Advisor

Deux alertes `unused_index` restent signalées sur `public.inspections` :

- `inspections_agent_id_idx` ;
- `inspections_materiel_id_idx`.

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

Le projet reste sain, la branche n'a aucun retard sur `main`, les volumes cœur sont stables et aucun changement matériel n'est visible dans les Advisors depuis le précédent relevé. Il n'y a donc pas de remédiation automatique sûre à appliquer sur Supabase dans ce passage.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne fusionne rien vers `main`, n'active pas la RLS stricte et n'effectue aucun déploiement de production.
