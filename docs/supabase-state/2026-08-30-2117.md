# RailOps — état GitHub / Supabase — 2026-08-30 21:17 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de `security/v150b2b-rls-ready`.

## GitHub

- `main` observé et laissé intact : `fd590aff348f52aade83f51e838f96457a14402c`.
- Branche observée avant ce relevé : `security/v150b2b-rls-ready` à `0464642777e2103ba25751b910a428f719d9bf62`.
- Comparaison fraîche `main...security/v150b2b-rls-ready` : **0 commits behind / 716 ahead** ; merge-base `fd590aff348f52aade83f51e838f96457a14402c`.
- PR #1 : ouverte, draft et non fusionnée.
- Garde-fou Git : aucun merge, rebase ou changement de `main` n'est effectué par ce relevé.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État observé : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`, région `eu-north-1`.

### RLS / policies — lecture seule

Comptages frais des policies sur les tables cœur :

- `agents` : 0 ;
- `chantiers` : 4 ;
- `deleted_ids` : 1 ;
- `inspections` : 0 ;
- `materiels` : 4 ;
- `prix_catalogue` : 1 ;
- `scans` : 4 ;
- `users` : 2.

Les huit tables ci-dessus ont `rls_enabled=true` lors du relevé.

### Security Advisor

Le contrôle frais conserve les familles déjà connues :

- 5 avis `RLS Enabled No Policy` de niveau `INFO` ;
- 33 avis `Signed-In Users Can Execute SECURITY DEFINER Function` de niveau `WARN` sur des RPC `public.railops_*` exécutables par `authenticated` ;
- 1 avis `Leaked Password Protection Disabled` de niveau `WARN`.

Ces alertes restent des signaux de diagnostic uniquement.
Aucune permission, fonction, policy, Auth ou RLS n'est modifiée automatiquement.

### Performance Advisor

Deux avis `Unused Index` de niveau `INFO` restent signalés sur `public.inspections` :

- `inspections_agent_id_idx` ;
- `inspections_materiel_id_idx`.

Aucun index ni changement de schéma n'est appliqué.

## Veille plateforme Supabase

Le changelog Supabase consulté le 2026-08-30 signale deux changements à surveiller sans action immédiate sur RailOps :

- à partir du 2026-10-30, les **nouvelles** tables `public` ne seront plus automatiquement exposées à la Data API ; les tables existantes conservent leurs grants actuels, donc aucun changement n'est requis sur le schéma RailOps actuel ;
- depuis le 2026-08-05, le pinning explicite de version dans `CREATE EXTENSION` / `ALTER EXTENSION ... UPDATE TO` est ignoré. Une recherche GitHub sur la branche n'a trouvé aucun `create extension`, donc aucun correctif n'est requis.

Références :

- https://supabase.com/changelog/45329-breaking-change-tables-not-exposed-to-data-and-graphql-api-automatically
- https://supabase.com/changelog/extension-version-pinning-ignored

## Diagnostic

La première version de ce relevé a fait échouer uniquement le contrat documentaire `v150b2b-snapshot-contract` car elle omettait la section RLS/policies obligatoire. La cause a été confirmée dans les logs CI ; les comptages ont ensuite été relus directement dans PostgreSQL avant correction. Aucune dérive applicative nouvelle n'est détectée : la branche reste alignée sur l'ascendance de `main`, le projet Supabase est sain et les avis Advisor restent dans les familles déjà documentées. Les changements de plateforme repérés ne nécessitent aucune migration immédiate pour l'état actuel de RailOps.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne fusionne rien vers `main`, n'active pas la RLS stricte et n'effectue aucun déploiement de production.

## Références Advisor

- RLS sans policy : https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy
- Fonctions `SECURITY DEFINER` exposées : https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable
- Protection des mots de passe compromis : https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection
- Index inutilisé : https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index
