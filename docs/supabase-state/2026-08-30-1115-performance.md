# Snapshot Supabase — performance — 2026-08-30 11:15 Europe/Paris

## Contexte

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`)
- État observé : `ACTIVE_HEALTHY`
- Lecture uniquement : aucun SQL, aucune migration, aucune modification de configuration, données, schéma, RLS, policies ou permissions.
- Branche GitHub de travail : `security/v150b2b-rls-ready` au head observé `b457d03353567cde8ff1814cbf6e6fad8e42ef73` avant ce snapshot.
- `main` observé et laissé intact à `37b216936a6692d54f82cbc004b30c936d13785a`.

## Avis performance observés

Le conseiller Supabase signale deux avis `unused_index` de niveau `INFO` :

1. `public.inspections.inspections_agent_id_idx` — index déclaré inutilisé selon les statistiques actuellement observées.
2. `public.inspections.inspections_materiel_id_idx` — index déclaré inutilisé selon les statistiques actuellement observées.

Référence de remédiation Supabase : `https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index`.

## Diagnostic et frontière de décision

Ces avis ne justifient **aucune suppression automatique** : l'absence d'utilisation observée peut dépendre du volume actuel, des requêtes réellement exécutées et de la fenêtre de statistiques. Retirer un index est une modification de schéma potentiellement régressive et sort donc du périmètre autonome RailOps.

Action retenue : documenter uniquement le constat. Toute suppression, remplacement ou création d'index devra être décidée explicitement après observation des requêtes et validation de l'impact.
