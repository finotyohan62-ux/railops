# RailOps — état GitHub / Supabase — 2026-08-31 03:16 Europe/Paris

Snapshot réalisé strictement en lecture seule côté Supabase.

## GitHub

- Branche de travail : `security/v150b2b-rls-ready`.
- Head observé avant changement : `35618a0ffb66f854db440361dba9289b4828286a`.
- `main` observé à `fd590aff348f52aade83f51e838f96457a14402c` et laissé intact.
- Divergence observée avant commit : **0 commits behind / 731 ahead**.
- Garde-fou explicite : aucun merge, rebase ou changement de `main`.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- Statut : `ACTIVE_HEALTHY`.
- Région : `eu-north-1`.
- PostgreSQL : `17.6.1.084`.
- Aucune écriture Supabase effectuée pendant ce relevé.

### RLS / policies — lecture seule

Comptage frais des policies sur les huit tables cœur :

- `agents` : 0 ; rls_enabled=true
- `chantiers` : 4 ; rls_enabled=true
- `deleted_ids` : 1 ; rls_enabled=true
- `inspections` : 0 ; rls_enabled=true
- `materiels` : 4 ; rls_enabled=true
- `prix_catalogue` : 1 ; rls_enabled=true
- `scans` : 4 ; rls_enabled=true
- `users` : 2 ; rls_enabled=true

### Security Advisor

- Observation fraîche : `observed_at=2026-08-31T01:17:21.460Z` pour les lints retournés par le Security Advisor.
- 5 avis `RLS Enabled No Policy` restent signalés sur `agents`, `inspections`, `materiel`, `railops_auth_throttle` et `railops_legacy_credentials`.
- Les avertissements connus concernant des fonctions `SECURITY DEFINER` exécutables par `authenticated` restent présents.
- La protection Supabase Auth contre les mots de passe compromis reste désactivée.
- Aucune famille nouvelle n'a été identifiée par rapport au dernier relevé documenté.

Ces alertes restent des signaux de diagnostic uniquement.
Aucune permission, fonction, policy, Auth ou RLS n'est modifiée automatiquement.

### Performance Advisor

- Observation fraîche : `observed_at=2026-08-31T01:17:28.517Z`.
- `inspections_agent_id_idx` reste signalé comme index inutilisé.
- `inspections_materiel_id_idx` reste signalé comme index inutilisé.
- Aucune famille nouvelle n'a été identifiée.

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

L'état observé est stable : projet Supabase sain, mêmes comptages RLS/policies et mêmes familles Advisor connues. Ce relevé ajoute les horodatages `observed_at` afin de distinguer clairement une lecture fraîche d'une alerte historique et de réduire les faux signaux lors des contrôles récurrents.

## Garde-fous

- Travail limité à `security/v150b2b-rls-ready`.
- Rappel : aucun merge, rebase ou changement de `main`.
- Aucune activation RLS stricte.
- Aucun déploiement production.
- Aucun changement de données, schéma, policy, permission, Auth ou règle métier.
- Aucun changement du comportement Import, Multi-chantier ou de la purge de vérification hebdomadaire.
