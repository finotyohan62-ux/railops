# RailOps — snapshot Supabase/GitHub 2026-08-23 21:14

Snapshot d’observation uniquement. Aucun changement de base, de policy, de RLS, de permission, de donnée ou de branche principale n’a été appliqué pendant ce relevé.

## GitHub

- Branche observée : `security/v150b2b-rls-ready`.
- PR #1 : ouverte, brouillon, non fusionnée, mergeable au moment du relevé.
- Head observé avant ce snapshot : `c116b942228909289b2410a10dd40770e6bdc7c6`.
- `main` observé : `283fca36c07f990ad80613d2aac97c2de3ef7bcf`.
- Comparaison `main...security/v150b2b-rls-ready` : branche divergente, **207 commits devant / 12 commits derrière**.
- Aucun merge/rebase n’a été tenté.

## Supabase

Projet observé : `railops` (`tbmzmmamaiftbbbuelgd`), état `ACTIVE_HEALTHY`, PostgreSQL 17.6.1.

### RLS / policies observées en lecture seule

Toutes les tables publiques listées par le relevé ont RLS activé. Nombre de policies observées :

- `agents` : 0 ;
- `app_config` : 1 ;
- `chantiers` : 4 ;
- `deleted_ids` : 1 ;
- `inspections` : 0 ;
- `materiel` : 0 ;
- `materiels` : 4 ;
- `prix_catalogue` : 1 ;
- `railops_auth_throttle` : 0 ;
- `railops_legacy_credentials` : 0 ;
- `scans` : 4 ;
- `users` : 2.

### Advisors

Le Security Advisor signale toujours notamment :

- RLS activée sans policy sur `public.agents`, `public.inspections`, `public.materiel`, `public.railops_auth_throttle` et `public.railops_legacy_credentials` ;
- plusieurs RPC `SECURITY DEFINER` exécutables par le rôle `authenticated` ;
- la protection Supabase Auth contre les mots de passe compromis désactivée.

Le Performance Advisor signale toujours deux clés étrangères non indexées sur `public.inspections` :

- `inspections_agent_id_fkey` ;
- `inspections_materiel_id_fkey`.

Ces alertes sont consignées comme état observé, pas comme instruction de remédiation. Toute modification de policy, droits d’exécution, Auth, index ou RLS reste hors périmètre sans validation explicite.

## Garde-fous confirmés

- `main` intact ;
- aucune migration appliquée ;
- aucune policy modifiée ;
- aucune donnée modifiée ;
- aucune activation supplémentaire de RLS ;
- aucun changement Import, Multi-chantier ou purge hebdomadaire ;
- aucune règle métier ou permission modifiée.
