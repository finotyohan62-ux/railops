# RailOps — snapshot Supabase lecture seule — 2026-08-31 01:17 Europe/Paris

## État projet

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`)
- Statut : `ACTIVE_HEALTHY`
- Région : `eu-north-1`
- PostgreSQL : `17.6.1.084`
- Aucun changement Supabase effectué pendant ce relevé.

## Advisors sécurité

État observé en lecture seule :

- 5 avis `RLS Enabled No Policy` sur `agents`, `inspections`, `materiel`, `railops_auth_throttle` et `railops_legacy_credentials`.
- Plusieurs fonctions `SECURITY DEFINER` restent exécutables par le rôle `authenticated` ; aucune permission n'a été modifiée.
- La protection Supabase Auth contre les mots de passe compromis reste désactivée ; aucune configuration Auth n'a été modifiée.

Ces éléments restent volontairement hors intervention automatique car leur remédiation touche à la sécurité, aux permissions ou au comportement d'authentification.

## Advisors performance

- `inspections_agent_id_idx` est signalé comme index inutilisé.
- `inspections_materiel_id_idx` est signalé comme index inutilisé.

Aucun index n'a été supprimé ou modifié : une telle action changerait le schéma et nécessite une décision explicite.

## Garde-fous respectés

- `main` observé à `fd590aff348f52aade83f51e838f96457a14402c` et laissé intact.
- Travail limité à `security/v150b2b-rls-ready`.
- Aucun merge ni déploiement production.
- Aucune activation RLS stricte.
- Aucun changement de données, schéma, policy, permission, règle métier, Import, Multi-chantier ou purge hebdomadaire.
