# RailOps — état GitHub / Supabase — 2026-08-24 03:17 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail : `security/v150b2b-rls-ready`.
- PR #1 : ouverte, en brouillon, non fusionnée ; head observé avant ce relevé : `bd22a9aa7ae9e0a2b20be3074ecb4131669c4464`.
- Vercel sur ce head : `success`.
- Comparaison fraîche avec `main` : **12 commits behind / 220 ahead**.
- Les historiques restent divergents ; aucun merge, rebase ou changement de `main` n'est effectué par ce relevé.
- Merge-base observée : `4b50df53ee449c4c907bae6a215672be3a2597d9`.
- `main` observé : `283fca36c07f990ad80613d2aac97c2de3ef7bcf`.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État : `ACTIVE_HEALTHY`.
- PostgreSQL : `17.6.1.084`.
- Région : `eu-north-1`.

### Security Advisor

Le relevé courant conserve les catégories déjà connues :

- cinq tables avec RLS activé sans policy : `agents`, `inspections`, `materiel`, `railops_auth_throttle`, `railops_legacy_credentials` ;
- avertissements sur des fonctions `SECURITY DEFINER` exécutables par `authenticated` ; ces alertes restent diagnostiques et ne justifient aucune modification automatique des permissions ;
- protection contre les mots de passe compromis désactivée.

Aucune correction Auth, permission, fonction, policy ou RLS n'est appliquée par ce snapshot.

### Performance Advisor

Deux informations restent signalées sur `public.inspections` :

- `inspections_agent_id_fkey` sans index couvrant ;
- `inspections_materiel_id_fkey` sans index couvrant.

Aucun index ni changement de schéma n'est appliqué.

### Journaux PostgreSQL (24 h)

Le relevé contient encore plusieurs erreurs historiques `column deleted_ids.supprimé_à does not exist`, la plus récente observée étant datée du 2026-08-23 à 16:44 UTC. Aucun nouvel événement de ce type n'apparaît dans les entrées plus récentes visibles lors de ce relevé ; aucune correction automatique n'est déduite de ce constat.

## Diagnostic

Aucune nouvelle catégorie d'alerte nécessitant une intervention automatique n'est déduite de ce relevé. La divergence GitHub et les points Supabase connus restent volontairement sans action tant qu'ils impliquent merge, permissions, RLS, Auth ou schéma.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne constitue pas une autorisation de fusionner la branche ni d'activer une RLS stricte.
