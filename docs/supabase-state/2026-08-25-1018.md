# RailOps — état GitHub / Supabase — 2026-08-25 10:18 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail : `security/v150b2b-rls-ready`.
- PR #1 : ouverte, en brouillon et non fusionnée.
- Head observé avant ce relevé : `926c148cdb45484c471ddaf2817d386c679541eb`.
- Comparaison fraîche avec `main` : **16 commits behind / 316 ahead** ; historiques divergents.
- `main` observé : `287265429c7484cc72b31871568f8c4baac1bfc1`.
- Merge-base observée : `4b50df53ee449c4c907bae6a215672be3a2597d9`.
- GitHub Actions sur le head observé : `v150B-2B checks` #492, `RailOps modules regression` #99 et `RailOps lifecycle regression` #331 sont `success`.
- Vercel : `success` sur le head observé.
- Garde-fou Git : aucun merge, rebase ou changement de `main` effectué.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`.
- Horodatage SQL du relevé RLS : `2026-08-25 08:18:14.581975+00`.
- Contrôle effectué uniquement via métadonnées projet, catalogue PostgreSQL et Advisors ; aucune écriture backend.

### RLS / policies — lecture seule

RLS est activé sur toutes les tables cœur inspectées. Comptage frais des policies :

- `agents` : 0 ;
- `chantiers` : 4 ;
- `deleted_ids` : 1 ;
- `inspections` : 0 ;
- `materiels` : 4 ;
- `prix_catalogue` : 1 ;
- `scans` : 4 ;
- `users` : 2 ;

### Security Advisor

Les familles déjà connues restent présentes :

- RLS activé sans policy sur `agents`, `inspections`, `materiel`, `railops_auth_throttle` et `railops_legacy_credentials` ;
- avertissements sur des fonctions `SECURITY DEFINER` exécutables par `authenticated` ;
- protection contre les mots de passe compromis désactivée.

Ces alertes restent des signaux de diagnostic uniquement. Aucune permission, fonction, policy, Auth ou RLS n'est modifiée automatiquement.

### Performance Advisor

Les deux informations déjà connues restent présentes sur `public.inspections` :

- `inspections_agent_id_fkey` sans index couvrant ;
- `inspections_materiel_id_fkey` sans index couvrant.

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

Aucune nouvelle famille Advisor ni dérive de comptage RLS/policies n'est visible par rapport au relevé précédent. La divergence Git reste à **16 commits behind** et la branche continue d'avancer uniquement par des changements réversibles de tests/diagnostics/documentation tant qu'une synchronisation avec `main` n'est pas explicitement validée.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne constitue pas une autorisation de fusionner la branche ni d'activer une RLS stricte.
