# RailOps — état GitHub / Supabase — 2026-08-25 08:17 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail : `security/v150b2b-rls-ready`.
- PR #1 : ouverte, en brouillon et non fusionnée.
- Head observé avant ce relevé : `575b04b20dfd9d64cef89d698165549b426a721f`.
- Comparaison fraîche avec `main` : **16 commits behind / 309 ahead** ; historiques divergents.
- `main` observé : `287265429c7484cc72b31871568f8c4baac1bfc1`.
- Merge-base observée : `4b50df53ee449c4c907bae6a215672be3a2597d9`.
- GitHub Actions : `v150B-2B checks` #478, `RailOps modules regression` #92 et `RailOps lifecycle regression` #324 sont `success` sur le head observé.
- Vercel : `success` sur le head observé.
- Garde-fou Git : aucun merge, rebase ou changement de `main` effectué.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`.
- Horodatage SQL du relevé RLS : `2026-08-25 06:22:26.825548+00`.
- Contrôle effectué uniquement via métadonnées projet, catalogue PostgreSQL, Advisors et logs ; aucune écriture backend.

### RLS / policies — lecture seule

RLS est activé sur toutes les tables cœur inspectées. Comptage frais des policies :

- `agents` : 0 ;
- `chantiers` : 4 ;
- `deleted_ids` : 1 ;
- `inspections` : 0 ;
- `materiels` : 4 ;
- `prix_catalogue` : 1 ;
- `scans` : 4 ;
- `users` : 2 ;

### Security Advisor

Les familles déjà connues restent présentes :

- RLS activé sans policy sur `agents`, `inspections`, `materiel`, `railops_auth_throttle` et `railops_legacy_credentials` ;
- avertissements sur des fonctions `SECURITY DEFINER` exécutables par `authenticated` ;
- protection contre les mots de passe compromis désactivée.

Aucune permission, fonction, policy, Auth ou RLS n'est modifiée.

### Performance Advisor

Les deux informations déjà connues restent présentes sur `public.inspections` :

- `inspections_agent_id_fkey` sans index couvrant ;
- `inspections_materiel_id_fkey` sans index couvrant.

Aucun index ni changement de schéma n'est appliqué.

### Logs PostgreSQL

La fenêtre de logs consultée couvre les dernières 24 h. Les refus RLS visibles sur `materiels` sont antérieurs à ce relevé ; la dernière occurrence correspondante retournée est horodatée `2026-08-24T23:02:28.854Z`. Aucune occurrence plus récente du même message n'apparaît dans les événements retournés. Ce constat est uniquement diagnostique et ne permet pas d'attribuer une cause ni de justifier une modification de policy ou de permission.

## Diagnostic

Aucune nouvelle dérive RLS/policies ou famille Advisor n'est visible par rapport au relevé de 07:13. La branche sécurité a avancé par des changements de test/documentation uniquement et reste 16 commits derrière `main`. Les erreurs RLS `materiels` actuellement visibles dans la fenêtre de logs sont conservées comme signal historique jusqu'à une reproduction datée et attribuable ; aucune correction backend n'est tentée.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne constitue pas une autorisation de fusionner la branche ni d'activer une RLS stricte.
