# RailOps — état GitHub / Supabase — 2026-08-29 22:18 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail observée avant ce snapshot : `security/v150b2b-rls-ready` au commit `0c6701d62df09f7d5835ba88af05608fa62e283c`.
- `main` observé et laissé intact : `37b216936a6692d54f82cbc004b30c936d13785a`.
- Comparaison fraîche `main...security/v150b2b-rls-ready` : **0 commits behind / 629 ahead**, statut `ahead`; merge-base `37b216936a6692d54f82cbc004b30c936d13785a`.
- Garde-fou Git : aucun merge, rebase, cherry-pick ou changement de `main` n'est effectué par ce relevé.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État observé : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`, région `eu-north-1`.

### Volumétrie cœur — lecture seule

- `agents` : 0 ;
- `chantiers` : 22 ;
- `deleted_ids` : 0 ;
- `inspections` : 0 ;
- `materiels` : 1689 ;
- `prix_catalogue` : 2 ;
- `scans` : 124 ;
- `users` : 34.

Ces valeurs sont uniquement un instantané de diagnostic. Aucune donnée n'a été créée, modifiée ou supprimée.

### RLS / policies — lecture seule

Le contrôle direct confirme RLS activé mais non forcé sur les tables cœur observées. Comptages de policies :

- `agents` : 0 ;
- `chantiers` : 4 ;
- `deleted_ids` : 1 ;
- `inspections` : 0 ;
- `materiels` : 4 ;
- `prix_catalogue` : 1 ;
- `scans` : 4 ;
- `users` : 2.

Aucune permission, fonction, policy, Auth ou RLS n'est modifiée automatiquement.

### Security Advisor

Familles observées :

- `RLS Enabled No Policy` sur plusieurs tables déjà connues, dont `agents` et `inspections` ;
- avertissements `authenticated_security_definer_function_executable` sur plusieurs fonctions `SECURITY DEFINER` exposées à `authenticated` ;
- protection contre les mots de passe compromis désactivée.

Ces alertes restent des signaux de diagnostic uniquement. Toute remédiation nécessite une décision explicite avant toute modification de sécurité.

### Performance Advisor

Deux alertes `unused_index` sont actuellement signalées sur `public.inspections` :

- `inspections_agent_id_idx` ;
- `inspections_materiel_id_idx`.

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

Le projet reste sain et la branche n'a aucun retard sur `main`. Les volumes cœur sont stables par rapport au relevé de 21:12 (`scans=124`, `materiels=1689`, `users=34`). Les Advisors restent inchangés dans le périmètre observé et ne justifient aucune action automatique sûre : les alertes sécurité demandent une décision explicite, et les deux index d'inspections sont conservés.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne fusionne rien vers `main`, n'active pas la RLS stricte et n'effectue aucun déploiement de production.
