# RailOps — état GitHub / Supabase — 2026-08-24 13:17 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail : `security/v150b2b-rls-ready`.
- PR #1 : ouverte, en brouillon, non fusionnée ; head observé avant ce relevé : `14437af1de04909140237e1ca6c5eb8542d527e9`.
- Comparaison fraîche avec `main` : **12 commits behind / 247 ahead**.
- Historiques toujours divergents ; aucun merge, rebase ou changement de `main` effectué.
- Merge-base observée : `4b50df53ee449c4c907bae6a215672be3a2597d9`.
- `main` observé : `283fca36c07f990ad80613d2aac97c2de3ef7bcf`.
- Statut Vercel du head observé : `success`.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`.
- Contrôle effectué uniquement par lectures SQL et Advisors ; aucune écriture backend.

### RLS / policies — lecture seule

Les huit tables cœur suivies ont RLS activé. Comptage des policies observées :

- `agents` : 0 ;
- `chantiers` : 4 ;
- `deleted_ids` : 1 ;
- `inspections` : 0 ;
- `materiels` : 4 ;
- `prix_catalogue` : 1 ;
- `scans` : 4 ;
- `users` : 2.

### Security Advisor

Les catégories observées restent stables par rapport au relevé de 11:17 :

- cinq tables avec RLS activé sans policy : `agents`, `inspections`, `materiel`, `railops_auth_throttle`, `railops_legacy_credentials` ;
- avertissements sur des fonctions `SECURITY DEFINER` exécutables par `authenticated` ;
- protection contre les mots de passe compromis désactivée.

Aucune permission, fonction, policy, Auth ou RLS n'est modifiée.

### Performance Advisor

Les deux informations déjà connues restent présentes sur `public.inspections` :

- `inspections_agent_id_fkey` sans index couvrant ;
- `inspections_materiel_id_fkey` sans index couvrant.

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

Aucune nouvelle catégorie d'alerte Supabase n'est apparue depuis le snapshot de 11:17. La branche est passée de 243 à 247 commits ahead tout en restant 12 commits behind `main`; la synchronisation reste volontairement non automatique.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne constitue pas une autorisation de fusionner la branche ni d'activer une RLS stricte.
