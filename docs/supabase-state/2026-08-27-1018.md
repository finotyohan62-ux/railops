# RailOps — état GitHub / Supabase — 2026-08-27 10:18 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail observée avant ce snapshot : `security/v150b2b-rls-ready` au commit `7f4ad058a31b944b8630aa9ab630a877440554ed`.
- `main` observé : `37b216936a6692d54f82cbc004b30c936d13785a`.
- Comparaison fraîche `main...security/v150b2b-rls-ready` : **2 commits behind / 463 ahead**, statut `diverged` ; merge-base `20f7e028ac5e3d0ac401d41ec3561af09e252694`.
- Garde-fou Git : aucun merge, rebase ou changement de `main` n'est effectué par ce relevé ; aucun cherry-pick n'est effectué non plus.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État observé : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`.

### Volumétrie cœur — lecture seule

- `agents` : 0 ;
- `chantiers` : 22 ;
- `deleted_ids` : 0 ;
- `inspections` : 0 ;
- `materiels` : 1689 ;
- `prix_catalogue` : 2 ;
- `scans` : 121 ;
- `users` : 34.

Ces valeurs sont uniquement un instantané de diagnostic. Aucune donnée n'a été créée, modifiée ou supprimée.

### RLS / policies — lecture seule

Le contrôle direct confirme RLS activé mais **non forcé** sur les tables cœur actives. Comptages de policies observés :

- `agents` : 0 ;
- `chantiers` : 4 ;
- `deleted_ids` : 1 ;
- `inspections` : 0 ;
- `materiels` : 4 ;
- `prix_catalogue` : 1 ;
- `scans` : 4 ;
- `users` : 2 ;

Aucune permission, fonction, policy, Auth ou RLS n'est modifiée automatiquement.

### Security Advisor

Les familles observées restent inchangées :

- `RLS Enabled No Policy` sur `agents`, `inspections`, `materiel`, `railops_auth_throttle` et `railops_legacy_credentials` ;
- avertissements `authenticated_security_definer_function_executable` sur plusieurs fonctions `SECURITY DEFINER` exposées à `authenticated` ;
- protection contre les mots de passe compromis désactivée.

Remédiations officielles signalées par Supabase :

- https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy
- https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable
- https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection

Ces alertes restent des signaux de diagnostic uniquement. Elles nécessitent une décision explicite avant toute modification de sécurité.

### Performance Advisor

Deux alertes `unindexed_foreign_keys` persistent sur `public.inspections` :

- `inspections_agent_id_fkey` ;
- `inspections_materiel_id_fkey`.

Remédiation officielle signalée par Supabase :

- https://supabase.com/docs/guides/database/database-linter?lint=0001_unindexed_foreign_keys

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

Aucune nouvelle famille d'alerte Supabase et aucune variation de volumétrie cœur n'ont été constatées depuis le snapshot du 2026-08-26 à 21:12. La dérive Git a progressé à 2 commits `main` non intégrés ; elle reste informative et ne déclenche aucune intégration automatique.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne fusionne rien vers `main`, n'active pas la RLS stricte et n'effectue aucun déploiement de production.
