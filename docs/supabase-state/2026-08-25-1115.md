# RailOps — état GitHub / Supabase

Snapshot du 2026-08-25 11:15 Europe/Paris. Relevé **metadata-only**, strictement en lecture seule : aucun nom d’agent, aucune référence matériel et aucun payload métier n’est consigné ici.

## GitHub

- Branche contrôlée : `security/v150b2b-rls-ready`.
- PR #1 : ouverte, draft, non fusionnée.
- Comparaison fraîche avec `main` : **16 commits behind / 327 ahead** (`status: diverged`).
- Aucun merge, rebase ou changement de `main` n’a été effectué.

## Supabase

Projet : `railops` (`tbmzmmamaiftbbbuelgd`). Contrôles strictement en lecture seule.

### RLS / policies — lecture seule

Comptage observé sur les huit tables cœur :

- `agents` : 0 ; RLS actif.
- `chantiers` : 4 ; RLS actif.
- `deleted_ids` : 1 ; RLS actif.
- `inspections` : 0 ; RLS actif.
- `materiels` : 4 ; RLS actif.
- `prix_catalogue` : 1 ; RLS actif.
- `scans` : 4 ; RLS actif.
- `users` : 2 ; RLS actif.

### Security Advisor

Le relevé courant signale notamment des tables avec RLS actif mais sans policy (`agents`, `inspections`, ainsi que d’autres tables hors noyau), plusieurs fonctions `SECURITY DEFINER` exécutables par le rôle `authenticated`, et la protection contre les mots de passe compromis désactivée.

Ces alertes restent des signaux de diagnostic uniquement. Aucune permission, fonction, policy, Auth ou RLS n'est modifiée automatiquement.

### Performance Advisor

Deux clés étrangères de `public.inspections` sont actuellement signalées sans index couvrant (`inspections_agent_id_fkey` et `inspections_materiel_id_fkey`).

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

La dernière occurrence précédemment relevée du message applicatif `new row violates row-level security policy for table "materiels"` était horodatée `2026-08-24T23:02:28.854Z`. Le correctif ciblé côté application pour les sauvegardes Agent utilise désormais une mise à jour de ligne existante plutôt qu’un upsert ; le test de régression dédié valide ce contrat de code. Ce constat ne constitue pas à lui seul un smoke-test utilisateur de bout en bout.

## Garde-fous

- aucune donnée Supabase modifiée ;
- aucune migration, policy, permission, fonction, Auth ou schéma modifié ;
- aucune activation RLS supplémentaire ;
- Import, Multi-chantier et purge hebdomadaire inchangés ;
- aucune règle métier modifiée ;
- aucun merge, rebase ou changement de `main`.
