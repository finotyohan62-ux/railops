# Snapshot GitHub / Supabase — 2026-08-25 11:15 Europe/Paris

Snapshot **metadata-only** et lecture seule. Il ne contient ni nom d’agent, ni référence matériel, ni payload métier.

## GitHub

- Branche contrôlée : `security/v150b2b-rls-ready`.
- PR #1 : ouverte, draft, non fusionnée.
- Comparaison avec `main` au début de la passe : **16 commits behind / 319 ahead** (`status: diverged`).
- `main` n’a pas été modifiée ; aucun merge/rebase n’a été tenté.

## Supabase

Projet `railops` contrôlé en lecture seule.

RLS/policies observées sur les huit tables cœur :

- `agents` : RLS actif, 0 policy ;
- `chantiers` : RLS actif, 4 policies ;
- `deleted_ids` : RLS actif, 1 policy ;
- `inspections` : RLS actif, 0 policy ;
- `materiels` : RLS actif, 4 policies ;
- `prix_catalogue` : RLS actif, 1 policy ;
- `scans` : RLS actif, 4 policies ;
- `users` : RLS actif, 2 policies.

## Triage des logs PostgreSQL

Dans la fenêtre de logs disponible, la dernière occurrence du message applicatif `new row violates row-level security policy for table "materiels"` est horodatée `2026-08-24T23:02:28.854Z`.

Aucune occurrence plus récente de ce même message n’est visible dans le relevé consulté. Les événements postérieurs observés sont des événements PostgreSQL de routine (checkpoint, réplication/logical decoding).

Ce signal reste **diagnostique uniquement** : il ne justifie aucune modification automatique de policy, permission, RLS, schéma ou règle métier. Toute correction du chemin d’écriture inventaire/matériel reste bloquée jusqu’à une décision explicite, car elle touche au comportement d’écriture et aux permissions.

## Garde-fous

- aucune donnée modifiée ;
- aucune migration, policy, permission, fonction ou schéma modifié ;
- aucune activation RLS stricte ;
- Import, Multi-chantier et purge hebdomadaire inchangés ;
- aucune règle métier modifiée.
