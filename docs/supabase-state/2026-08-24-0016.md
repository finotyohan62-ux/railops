# RailOps — état GitHub / Supabase — 2026-08-24 00:16 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail : `security/v150b2b-rls-ready`.
- PR #1 : ouverte, en brouillon, non fusionnée ; head observé `962e415ed37c12bc0b516d5429c0da90dbf50a93` avant ce relevé.
- Comparaison fraîche avec `main` : **12 commits behind / 213 ahead**.
- Les historiques restent divergents ; aucun merge, rebase ou changement de `main` n'est effectué par ce relevé.
- Merge-base observée : `4b50df53ee449c4c907bae6a215672be3a2597d9`.
- `main` observé : `283fca36c07f990ad80613d2aac97c2de3ef7bcf`.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État : `ACTIVE_HEALTHY`.
- PostgreSQL : `17.6.1.084`.
- Région : `eu-north-1`.

### Security Advisor

Le relevé courant conserve notamment :

- cinq tables avec RLS activé sans policy : `agents`, `inspections`, `materiel`, `railops_auth_throttle`, `railops_legacy_credentials` ;
- plusieurs fonctions `SECURITY DEFINER` exécutables par le rôle `authenticated` ; ce signal reste diagnostique et ne suffit pas à conclure qu'un RPC est mal autorisé, les contrôles internes de rôle et de périmètre devant être évalués fonction par fonction ;
- protection contre les mots de passe compromis désactivée.

Aucune correction de permission, fonction, policy ou Auth n'est appliquée par ce snapshot.

### Performance Advisor

Deux informations sont toujours signalées sur `public.inspections` :

- `inspections_agent_id_fkey` sans index couvrant ;
- `inspections_materiel_id_fkey` sans index couvrant.

Aucun index ni changement de schéma n'est appliqué par ce snapshot.

## Diagnostic

Aucune nouvelle anomalie nécessitant une intervention automatique n'est déduite de ce relevé. La divergence GitHub et les avertissements Supabase connus restent des points à traiter uniquement avec validation explicite lorsqu'ils impliquent merge, permissions, RLS, Auth ou schéma.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne constitue pas une autorisation de fusionner la branche ni d'activer une RLS stricte.
