# RailOps — état GitHub / Supabase — 2026-08-25 04:17 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail : `security/v150b2b-rls-ready`.
- PR #1 : ouverte, en brouillon et non fusionnée.
- Head observé avant ce relevé : `9368b17cf3d122a02750cfa35ce287570ba20776`.
- Comparaison fraîche avec `main` au début de la passe : **16 commits behind / 295 ahead** ; historiques divergents.
- `main` observé : `287265429c7484cc72b31871568f8c4baac1bfc1`.
- Merge-base observée : `4b50df53ee449c4c907bae6a215672be3a2597d9`.
- Garde-fou Git : aucun merge, rebase ou changement de `main` effectué.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`.
- Horodatage SQL du relevé RLS/policies : `2026-08-25 02:17:04.353041+00`.
- Contrôle effectué uniquement via métadonnées projet, catalogue PostgreSQL, Advisors et logs ; aucune écriture backend.

### RLS / policies — lecture seule

RLS est activé sur toutes les tables listées ci-dessous. Comptage frais des policies observées :

- `agents` : 0 ;
- `chantiers` : 4 ;
- `deleted_ids` : 1 ;
- `inspections` : 0 ;
- `materiel` : 0 ;
- `materiels` : 4 ;
- `prix_catalogue` : 1 ;
- `railops_auth_throttle` : 0 ;
- `railops_legacy_credentials` : 0 ;
- `scans` : 4 ;
- `users` : 2 ;

### Security Advisor

Familles observées :

- RLS activé sans policy sur `agents`, `inspections`, `materiel`, `railops_auth_throttle` et `railops_legacy_credentials` ;
- avertissements sur des fonctions `SECURITY DEFINER` exécutables par `authenticated` ;
- protection contre les mots de passe compromis désactivée.

Aucune permission, fonction, policy, Auth ou RLS n'est modifiée.

### Performance Advisor

Les deux informations déjà connues restent présentes sur `public.inspections` :

- `inspections_agent_id_fkey` sans index couvrant ;
- `inspections_materiel_id_fkey` sans index couvrant.

Aucun index ni changement de schéma n'est appliqué.

### Logs Postgres — signal à confirmer humainement

Le relevé des logs sur les dernières 24 heures contient des refus RLS explicites :

- plusieurs `new row violates row-level security policy for table "materiels"` entre `2026-08-24 22:48:38 UTC` et `23:02:28 UTC` environ ;
- plusieurs `new row violates row-level security policy for table "users"` autour de `2026-08-24 20:12:56–20:13:02 UTC`.

Ces lignes attestent de tentatives d'écriture refusées, mais les logs seuls ne permettent pas d'identifier avec certitude l'action utilisateur exacte ni de conclure à une régression actuelle. Aucune correction de policy, permission ou flux d'écriture n'est tentée automatiquement ; un smoke-test humain ciblé sera nécessaire avant toute décision fonctionnelle ou sécurité.

## Diagnostic

La structure RLS/policies et les familles Advisors restent cohérentes avec le relevé de 03:52. Le nouveau signal de cette passe est la présence, dans les logs 24 h, de refus RLS sur `materiels` et `users`. Comme la branche sécurité est toujours **16 commits derrière `main`**, aucune interprétation causale ni synchronisation automatique n'est tentée.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne constitue pas une autorisation de fusionner la branche ni d'activer une RLS stricte.
