# RailOps — état GitHub / Supabase — 2026-08-26 04:14 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail observée : `security/v150b2b-rls-ready` au commit `ce01f47575b3e45fffb9fea2a33b178f390cc2e0` avant ce snapshot.
- `main` observé : `e89c57c995fe0661cffcdbfcf88b9f30a408a093`.
- Comparaison fraîche `main...security/v150b2b-rls-ready` : **1 commit behind / 373 ahead**, statut `diverged` ; merge-base `20f7e028ac5e3d0ac401d41ec3561af09e252694`.
- Le seul commit `main` absent de la branche reste le rafraîchissement de déploiement `e89c57c995fe0661cffcdbfcf88b9f30a408a093`. Aucun merge, rebase, cherry-pick ni changement de `main` n'est effectué.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État observé : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`.

### RLS — lecture seule

RLS est actuellement activé sur les tables cœur vérifiées :

- `chantiers` : activé, non forcé ;
- `deleted_ids` : activé, non forcé ;
- `materiels` : activé, non forcé ;
- `prix_catalogue` : activé, non forcé ;
- `scans` : activé, non forcé ;
- `users` : activé, non forcé.

Ce relevé constate uniquement l'état réel actuel. Il n'active, ne désactive et ne modifie aucune policy ou RLS.

### Security Advisor

Le relevé courant conserve les catégories connues :

- RLS activé sans policy sur `agents`, `inspections`, `materiel`, `railops_auth_throttle` et `railops_legacy_credentials` ;
- avertissements `authenticated_security_definer_function_executable` sur plusieurs fonctions `SECURITY DEFINER` exposées à `authenticated` ;
- protection contre les mots de passe compromis désactivée.

Références Supabase :

- RLS sans policy : https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy
- SECURITY DEFINER exécutable par `authenticated` : https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable
- protection mots de passe compromis : https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection

Aucune correction de sécurité n'est appliquée automatiquement.

### Performance Advisor

Deux informations restent signalées sur `public.inspections` :

- `inspections_agent_id_fkey` sans index couvrant ;
- `inspections_materiel_id_fkey` sans index couvrant.

Référence : https://supabase.com/docs/guides/database/database-linter?lint=0001_unindexed_foreign_keys

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

Aucune nouvelle catégorie d'alerte n'est apparue par rapport au snapshot précédent. La branche reste volontairement séparée de `main` ; l'écart d'un commit côté `main` est documenté comme information de compatibilité uniquement.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne fusionne rien vers `main` et n'effectue aucun déploiement de production.
