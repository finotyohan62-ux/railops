# RailOps — état GitHub / Supabase — 2026-08-31 10:14 Europe/Paris

Relevé strictement en lecture seule.

## GitHub
- Branche : `security/v150b2b-rls-ready`
- Head observé avant changement : `2646bdc85a17e081338047b6b70d7181824c3fc7`
- `main` : `fd590aff348f52aade83f51e838f96457a14402c`
- Divergence observée : **0 behind / 748 ahead**
- `main` laissé intact ; aucun merge ni déploiement production.

## Supabase
- Projet : `railops` (`tbmzmmamaiftbbbuelgd`)
- Santé/version : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084` (engine 17, canal GA).

### RLS / policies — lecture seule
- `agents` : policy_count=0 ; rls_enabled=true
- `chantiers` : policy_count=4 ; rls_enabled=true
- `deleted_ids` : policy_count=1 ; rls_enabled=true
- `inspections` : policy_count=0 ; rls_enabled=true
- `materiels` : policy_count=4 ; rls_enabled=true
- `prix_catalogue` : policy_count=1 ; rls_enabled=true
- `scans` : policy_count=4 ; rls_enabled=true
- `users` : policy_count=2 ; rls_enabled=true

### Security Advisor
- Observation fraîche : `2026-08-31T08:15:09.092Z` pour les lints de base de données.
- Familles inchangées : 5 tables avec RLS activée sans policy, fonctions `SECURITY DEFINER` exécutables par `authenticated`, et protection des mots de passe compromis désactivée.
- Ces alertes restent des signaux de diagnostic uniquement.
- Aucune permission, fonction, policy, Auth ou RLS n'est modifiée automatiquement.

### Performance Advisor
- Observation fraîche : `2026-08-31T08:16:57.058Z`.
- Deux index restent signalés comme inutilisés : `inspections_agent_id_idx` et `inspections_materiel_id_idx`.
- Aucun index ni changement de schéma n'est appliqué.

## Diagnostic
- Aucune nouvelle famille d'alerte n'a été observée par rapport au relevé précédent.
- L'état RLS/policies des huit tables cœur est stable.
- Les sujets sécurité/Auth et les index inutilisés restent hors périmètre automatique car leur remédiation toucherait aux permissions, à Auth ou au schéma.

## Garde-fous
- aucun merge, rebase ou changement de `main` ;
- aucune activation de RLS stricte ;
- aucune écriture Supabase, donnée, schéma, permission ou règle métier ;
- aucun changement Import, Multi-chantier ou purge hebdomadaire.
