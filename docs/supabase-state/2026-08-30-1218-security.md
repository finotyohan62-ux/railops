# Supabase state — 2026-08-30 12:18 Europe/Paris

## Scope

Read-only diagnostic snapshot for RailOps while working only on `security/v150b2b-rls-ready`.

- Branch head inspected before this snapshot: `7fc6c970f7d73ec4bf37c8bca470bc511d1f0559`.
- `main` observed at `37b216936a6692d54f82cbc004b30c936d13785a` and left untouched.
- Supabase project `railops` (`tbmzmmamaiftbbbuelgd`) status: `ACTIVE_HEALTHY`.
- No database, Auth, RLS, policy, permission, schema, data, Edge Function, or project-setting write was performed.

## Security advisor observations

The current advisor output still contains the already-known categories:

- `rls_enabled_no_policy` (`INFO`) on public tables including `agents`, `inspections`, `materiel`, `railops_auth_throttle`, and `railops_legacy_credentials`.
- `authenticated_security_definer_function_executable` (`WARN`) on several `public.railops_*` RPC functions callable by the `authenticated` role.
- `auth_leaked_password_protection` (`WARN`): leaked-password protection is disabled.

These items are diagnostic only in this autonomous pass. Correcting them would alter security, permissions, authentication behavior, or the intended staged RLS rollout and therefore requires explicit approval before any change.

## Performance advisor observations

The performance advisor still reports two `unused_index` (`INFO`) items:

- `public.inspections.inspections_agent_id_idx`
- `public.inspections.inspections_materiel_id_idx`

No index was removed or altered. Schema changes remain outside the autonomous low-risk scope.

## Guardrails confirmed

No change was made to Import, Multi-chantier, weekly verification purge behavior, business rules, permissions, strict RLS activation, production deployment, or merge state.
