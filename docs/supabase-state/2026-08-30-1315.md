# RailOps — état GitHub / Supabase — 2026-08-30 13:15 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail observée avant ce snapshot : `security/v150b2b-rls-ready` au commit `d396672dfbd04921bf5c1857381d7cee8f0c177d`.
- `main` observé et laissé intact : `37b216936a6692d54f82cbc004b30c936d13785a`.
- Comparaison fraîche `main...security/v150b2b-rls-ready` : **0 commits behind / 680 ahead**, statut `ahead`; merge-base `37b216936a6692d54f82cbc004b30c936d13785a`.
- PR #1 observée `open`, `draft=true`, `merged=false`; aucun changement d’état n’a été effectué.
- Garde-fou Git : aucun merge, rebase ou changement de `main` n'est effectué par ce relevé.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État observé : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`, région `eu-north-1`.
- Volumétrie cœur observée en lecture seule : `scans=124`, `materiels=1689`, `users=34`.

### RLS / policies — lecture seule

Comptages de policies observés sur les tables cœur :

- `agents` : 0 ;
- `chantiers` : 4 ;
- `deleted_ids` : 1 ;
- `inspections` : 0 ;
- `materiels` : 4 ;
- `prix_catalogue` : 1 ;
- `scans` : 4 ;
- `users` : 2.

Aucune permission, fonction, policy, Auth ou RLS n'est modifiée automatiquement.

### Security Advisor

Le contrôle frais conserve les catégories déjà connues : tables avec RLS activé sans policy, fonctions `SECURITY DEFINER` exécutables par `authenticated`, et protection contre les mots de passe compromis désactivée.

Ces alertes restent des signaux de diagnostic uniquement. Toute remédiation nécessite une décision explicite avant toute modification de sécurité.

### Performance Advisor

Deux alertes `unused_index` restent signalées sur `public.inspections` :

- `inspections_agent_id_idx` ;
- `inspections_materiel_id_idx`.

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

Le projet Supabase reste `ACTIVE_HEALTHY`, la branche est à jour par rapport à `main` et les volumes cœur restent stables. Le défaut constaté pendant ce passage était documentaire : `docs/supabase-state/2026-08-30-1016.md` ne respectait pas le format du snapshot canonique vérifié par `tests/v150b2b-snapshot-contract.test.js`. Ce nouveau relevé restaure le contrat sans toucher au runtime, aux données ou à la sécurité.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne fusionne rien vers `main`, n'active pas la RLS stricte et n'effectue aucun déploiement de production.
