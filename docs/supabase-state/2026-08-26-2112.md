# RailOps — état GitHub / Supabase — 2026-08-26 21:12 Europe/Paris

Relevé strictement en lecture seule destiné au diagnostic de la branche `security/v150b2b-rls-ready`.

## GitHub

- Branche de travail observée avant ce snapshot : `security/v150b2b-rls-ready` au commit `8a077eb3d1b99c9253ba804e89e4781c0ffc0fee`.
- `main` observé : `e89c57c995fe0661cffcdbfcf88b9f30a408a093`.
- Comparaison fraîche `main...security/v150b2b-rls-ready` : **1 commit behind / 430 ahead**, statut `diverged` ; merge-base `20f7e028ac5e3d0ac401d41ec3561af09e252694`.
- Le commit `main` absent de la branche reste `e89c57c995fe0661cffcdbfcf88b9f30a408a093`, le rafraîchissement de déploiement du correctif de synchronisation Agent ; aucun merge, rebase ou changement de `main` n'est effectué et aucun cherry-pick n'est effectué non plus.

## Supabase

- Projet : `railops` (`tbmzmmamaiftbbbuelgd`).
- État observé : `ACTIVE_HEALTHY`, PostgreSQL `17.6.1.084`.

### Volumétrie cœur — lecture seule

Comptages observés sur les huit tables suivies :

- `agents` : 0 ;
- `chantiers` : 22 ;
- `deleted_ids` : 0 ;
- `inspections` : 0 ;
- `materiels` : 1689 ;
- `prix_catalogue` : 2 ;
- `scans` : 121 ;
- `users` : 34.

Ces valeurs sont uniquement un instantané de diagnostic. Aucune donnée n'a été créée, modifiée ou supprimée par ce relevé.

### RLS / policies — lecture seule

Le contrôle direct confirme RLS activé et non forcé sur `chantiers`, `deleted_ids`, `materiels`, `prix_catalogue`, `scans` et `users`. Aucun changement de policy ou de mode RLS n'est appliqué.

### Security Advisor

Les familles observées restent celles déjà documentées :

- `RLS Enabled No Policy` sur `agents`, `inspections`, `materiel`, `railops_auth_throttle` et `railops_legacy_credentials` ;
- avertissements `authenticated_security_definer_function_executable` sur plusieurs fonctions `SECURITY DEFINER` exposées à `authenticated` ;
- protection contre les mots de passe compromis désactivée.

Remédiations officielles signalées par Supabase :

- https://supabase.com/docs/guides/database/database-linter?lint=0008_rls_enabled_no_policy
- https://supabase.com/docs/guides/database/database-linter?lint=0029_authenticated_security_definer_function_executable
- https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection

Ces alertes restent des signaux de diagnostic uniquement. Aucune permission, fonction, policy, Auth ou RLS n'est modifiée automatiquement.

### Performance Advisor

Le contrôle en lecture seule confirme toujours deux alertes `unindexed_foreign_keys` sur `public.inspections` :

- `inspections_agent_id_fkey` ;
- `inspections_materiel_id_fkey`.

Remédiation officielle signalée par Supabase :

- https://supabase.com/docs/guides/database/database-linter?lint=0001_unindexed_foreign_keys

Aucun index ni changement de schéma n'est appliqué.

## Diagnostic

Aucune nouvelle famille d'alerte Supabase n'a été constatée. La volumétrie observée est nettement plus élevée que dans le snapshot de 18:16, ce qui est consigné comme état réel courant sans interprétation métier automatique. La branche reste volontairement séparée de `main` avec un seul commit `main` non intégré.

## Garde-fous

Ce relevé ne modifie aucune donnée, migration, policy, permission, fonction, schéma, règle métier, Import, logique Multi-chantier ou purge hebdomadaire. Il ne fusionne rien vers `main`, n'active pas la RLS stricte et n'effectue aucun déploiement de production.
