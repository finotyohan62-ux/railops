## 2026-08-23 — diagnostic automatique de dérive de branche 07:20 Europe/Paris

- État contrôlé avant modification : PR #1 toujours ouverte en brouillon sur `security/v150b2b-rls-ready`, non fusionnée ; comparaison fraîche avec `main` = **12 commits behind / 158 ahead** au départ. Supabase `railops` vérifié `ACTIVE_HEALTHY` sous PostgreSQL 17.6.1 ; Security Advisor relu en lecture seule, sans correction backend.
- Amélioration réversible limitée à la CI/tests : ajout de `tests/v150b2b-ci-branch-drift.test.js` puis d’un step `Report branch drift (non-blocking)` dans `.github/workflows/v150b2b-checks.yml`. Sur les pushes de la branche sécurité, la CI récupère `origin/main`, calcule `behind/ahead` via `git rev-list`, écrit le diagnostic dans `GITHUB_STEP_SUMMARY` et reste explicitement non bloquante ; aucun merge/rebase n’est exécuté.
- Commits : `2f29417addfb680602c52b5017754df7c0678df8` (`test: require non-blocking branch drift CI diagnostic`) et `a0a4d9e7aabe72f58a6b04c826ec1800de75be6f` (`ci: report security branch drift without blocking`).
- Vérification fraîche : GitHub Actions `v150B-2B checks` run #180 et `RailOps lifecycle regression` run #165 sont tous deux `success` sur `a0a4d9e7aabe72f58a6b04c826ec1800de75be6f`; Vercel est également `success`. Comparaison après modification : **12 commits behind / 160 ahead**.
- Aucun code applicatif, règle métier, permission, migration, donnée Supabase, RLS, Import, Multi-chantier ou purge hebdomadaire n’a été modifié ; `main` reste intact et aucun merge/rebase n’a été tenté.
- Point en attente inchangé : la synchronisation avec les 12 commits de `main` et les smoke-tests humains restent soumis à validation explicite ; aucun nouveau choix produit n’a été introduit.
