## 2026-08-23 — garde-fou CI non-mutant 08:16 Europe/Paris

- État contrôlé avant modification : PR #1 ouverte en brouillon, non fusionnée, sur `security/v150b2b-rls-ready`; comparaison fraîche avec `main` = **12 commits behind / 161 ahead**. Vercel était `success` sur le head de départ. Aucun merge/rebase n'a été tenté.
- Supabase contrôlé en lecture seule : projet `railops` `ACTIVE_HEALTHY` sous PostgreSQL 17.6.1 ; Security Advisor relu sans appliquer de correction, migration, policy ou permission. Les alertes RLS/`SECURITY DEFINER`/protection mots de passe restent diagnostiques uniquement.
- Amélioration réversible limitée aux tests : ajout de `tests/v150b2b-ci-nonmutation.test.js`, qui verrouille `contents: read`, `persist-credentials: false`, l'absence de `git push/merge/rebase/reset --hard`, de merge via `gh`, de référence à la migration RLS stricte et de commandes de déploiement/migration Supabase dans la CI v150B-2B.
- Commit d'amélioration : `bf5debcf72400ea433ba4a0cbc72846d087d8b63` (`test: guard v150B2B CI against repository mutation`).
- Vérification fraîche : fichier relu depuis la branche ; `node --check` puis le test ciblé exécutés localement sur une reconstruction exacte du workflow courant, résultat `PASS: v150B-2B CI remains read-only and non-mutating`.
- Aucun code applicatif, règle métier, permission, migration, donnée Supabase, RLS, Import, Multi-chantier ou purge hebdomadaire n'a été modifié. Aucun nouveau point nécessitant une décision utilisateur n'a été introduit ; les points en attente historiques restent inchangés.
