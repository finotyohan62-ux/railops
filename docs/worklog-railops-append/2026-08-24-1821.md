## 2026-08-24 — classification des modules extraits dans le diagnostic de dérive 18:21 Europe/Paris

- État contrôlé avant modification : PR #1 toujours ouverte en brouillon sur `security/v150b2b-rls-ready`, non fusionnée ; comparaison fraîche avec `main` = **14 commits behind / 264 ahead**. Supabase `railops` est `ACTIVE_HEALTHY` sous PostgreSQL 17.6.1.084 ; Security Advisor relu en lecture seule, sans correction backend.
- Diagnostic : les 14 commits absents de la branche incluent `css/railops.css` et plusieurs modules `js/`, mais le rapport de dérive CI ne classait comme « app » que `index.html` et les JS/HTML racine ; ces fichiers extraits pouvaient donc être comptés à tort dans « other ».
- Cycle rouge/vert : `aff37da2dd1ed767fd91b5f0bbe76e88f967dd80` (`test: classify extracted app modules in branch drift`) a fait échouer `v150B-2B checks` run #390 avec l’assertion attendue ; `7e81b69c0b3af692a8acbf0ac73a5816570dc443` (`ci: classify extracted app modules in drift report`) ajoute uniquement `css/` et `js/` à la catégorie app.
- Vérification fraîche : sur `7e81b69c0b3af692a8acbf0ac73a5816570dc443`, `v150B-2B checks` run #392, `RailOps lifecycle regression` run #275 et `RailOps modules regression` run #45 sont tous `success`; Vercel est également `success`.
- Garde-fous : aucun code applicatif, règle métier, permission, migration, donnée Supabase, RLS, Import, logique Multi-chantier ou purge hebdomadaire n’a été modifié ; `main` reste intact et aucun merge/rebase n’a été tenté.
- Point en attente inchangé : la branche reste **14 commits derrière `main`** ; synchronisation et smoke-tests humains restent soumis à validation explicite. Aucun nouveau choix produit n’a été introduit.
