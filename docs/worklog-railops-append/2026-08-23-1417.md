## 2026-08-23 — erreur preview sûre et relançable 14:17 Europe/Paris

- État contrôlé avant modification : PR #1 toujours ouverte en brouillon sur `security/v150b2b-rls-ready`, non fusionnée ; comparaison fraîche avec `main` = **12 commits behind / 181 ahead** au départ. Supabase `railops` reste `ACTIVE_HEALTHY` sous PostgreSQL 17.6.1 ; Security Advisor relu en lecture seule, sans correction backend.
- Amélioration UX réversible limitée à la preview : `v150b2b-harness-core.js` fournit désormais un rendu d’erreur testé qui échappe le message avant insertion HTML, conserve `role="alert"` / `aria-live="assertive"`, présente l’erreur dans une carte lisible et propose un bouton `Réessayer`. `v150b2b-test.html` utilise ce renderer et garde un fallback texte si le module cœur lui-même ne charge pas.
- Cycle rouge/vert : `7e7fa1f183c7a5434c1f35c86cdfac95a7c286c0` pose le test de sécurité du message d’erreur et fait échouer `v150B-2B checks` run #224 comme attendu ; `015dee641a31abecb09a8ce9617f55491d7b3fb1` ajoute le renderer. Le test de câblage `b58f8973c172b61601327f0e5faf01d6b1e0ab4f` échoue ensuite volontairement (run #228) avant l’intégration.
- Diagnostic après intégration : le run #230 a détecté uniquement une violation du contrat de cache-bust (core `150b2b12`, autres adaptateurs `150b2b11`). La cause a été corrigée sans changement runtime métier via `fbb15efad0532882735bc1aa8fba25ac42ec76ce` puis `2ffbc2b9f9adba48a663d711265120dce594e5d6`, qui alignent tous les tags preview sur `150b2b12` et le test correspondant.
- Vérification fraîche : GitHub Actions `v150B-2B checks` run #234 et `RailOps lifecycle regression` run #192 sont tous deux `success` sur `2ffbc2b9f9adba48a663d711265120dce594e5d6`; Vercel est également `success`. Comparaison avant journalisation : **12 commits behind / 187 ahead**.
- Aucun changement de règle métier, permission, migration, donnée Supabase, RLS, Import, Multi-chantier ou purge hebdomadaire ; `main` reste intact et aucun merge/rebase n’a été tenté. Aucun nouveau choix utilisateur n’a été introduit ; les points historiques nécessitant validation restent inchangés.

> Note de traçabilité : le connecteur de contenu remplace les fichiers complets et `docs/worklog-railops.md` dépasse désormais la taille de réponse sûre ; cette entrée est donc conservée comme fragment d’append, sans risque de tronquer le journal principal, en attendant une opération d’append/consolidation atomique.
