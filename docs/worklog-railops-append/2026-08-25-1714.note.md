## 2026-08-25 — triage en lecture seule de la dérive 17:14 Europe/Paris

- État contrôlé avant modification : PR #1 ouverte en brouillon et non fusionnée sur `security/v150b2b-rls-ready`; comparaison fraîche avec `main` = **17 commits behind / 346 ahead** au début de la passe. Supabase `railops` est `ACTIVE_HEALTHY` sous PostgreSQL 17.6.1.084.
- Amélioration réversible limitée à la documentation : `docs/v150b2b-verification-guide.md` documente désormais les commandes de triage non mutantes `git log HEAD..origin/main` et `git diff` depuis le `merge-base`, ainsi que l'arrêt explicite de toute résolution automatique si `main` touche runtime, backend, Import, Multi-chantier, purge, permissions ou règles métier.
- Commit d'amélioration : `24abeb175ffd9b62107d8bed704a64a4df2b2402` (`docs: add read-only branch drift triage`). Vercel est `success` sur ce commit ; aucun statut GitHub Actions n'était encore exposé par l'API au moment du relevé.
- Garde-fous : aucun code runtime RailOps, donnée, migration, policy, permission, RLS, règle métier, Import, logique Multi-chantier ou purge hebdomadaire n'a été modifié ; `main` reste intact et aucun merge/rebase n'a été tenté.
- Point en attente historique inchangé : la synchronisation avec les 17 commits de `main` et les smoke-tests humains restent soumis à validation explicite. Cette note conserve la trace de la passe ; la consolidation dans le journal primaire reste à effectuer avant de considérer la journalisation totalement conforme.
