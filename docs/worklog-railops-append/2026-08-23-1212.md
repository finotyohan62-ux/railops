## 2026-08-23 — fichiers `main` visibles dans le diagnostic de dérive 12:12 Europe/Paris

- État contrôlé avant modification : PR #1 toujours ouverte en brouillon sur `security/v150b2b-rls-ready`, non fusionnée ; comparaison fraîche avec `main` = **12 commits behind / 175 ahead** au départ. Supabase `railops` est `ACTIVE_HEALTHY` sous PostgreSQL 17.6.1 ; contrôle RLS/policies effectué en lecture seule, avec RLS actif sur les huit tables cœur suivies.
- Amélioration réversible limitée à la CI de diagnostic : le résumé `Report branch drift (non-blocking)` affiche désormais jusqu’à 20 fichiers modifiés sur `main` mais absents de la branche (`git diff --name-only HEAD..origin/main`), pour faciliter le tri de compatibilité sans merge/rebase ni mutation du dépôt.
- Cycle rouge/vert confirmé par GitHub Actions : `02bf9bb02a1101156eea8f5747ba0b4e2d4ae6d8` (`test: require main-only drift file summary`) fait échouer `v150B-2B checks` run #212 comme attendu ; `15f1105836187d19b046cddced9bd69bcde6e851` (`ci: summarize main-only drift files`) rend `v150B-2B checks` run #214 et `RailOps lifecycle regression` run #182 tous deux `success`. Vercel est également `success` sur le commit vert.
- État après modification : branche toujours divergente, **12 commits behind / 177 ahead** avant journalisation ; `main` reste intact et aucun merge/rebase n’a été tenté.
- Aucun code applicatif, règle métier, permission, migration, donnée Supabase, RLS, Import, Multi-chantier ou purge hebdomadaire n’a été modifié.
- Point en attente inchangé : la synchronisation avec les 12 commits de `main` et les smoke-tests humains restent soumis à validation explicite ; aucun nouveau choix produit n’a été introduit par cette passe.
