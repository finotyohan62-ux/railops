## 2026-09-02 — import registre multi-chantier tolérant 01:49 Europe/Paris

- Diagnostic sur le registre DPI2455 : le lecteur v145 parcourt bien tous les onglets et identifie `INVENTAIRE` comme source structurée, mais son contrôle qualité bloquait l’import sur un total annoncé obsolète (`548`) et deux doublons exacts dans le même emplacement alors que `735` références uniques restaient déterminables sans ambiguïté.
- Cycle de correction : garde-fou `tests/v150b2b-register-import-quality.test.js` introduit en rouge puis adapté au contrat de compatibilité ; correctif runtime au commit `25ecdcba2c95d67cf85444f1fc0e1640e3412c37` dans `js/core/lifecycle.js`.
- Comportement : chaque `SheetName` reste inspecté ; uniquement pour les feuilles structurées, les doublons exacts même référence/même site sont neutralisés dans une copie mémoire et un compteur d’en-tête obsolète est aligné sur le nombre unique pour permettre l’analyse v145. Le fichier source n’est jamais modifié.
- Garde-fou d’ambiguïté conservé : si une même référence est réellement affectée à plusieurs sites dans la feuille source, aucune normalisation automatique n’est appliquée et le blocage v145 reste actif.
- Les règles métier v145 restent seules responsables de la prévisualisation, du mapping des sous-chantiers, des créations/déplacements, des écritures et de l’historique ; aucune modification Supabase, RLS, schéma, permission ou purge hebdomadaire.
- Vérification du correctif `25ecdcba2c95d67cf85444f1fc0e1640e3412c37` : le nouveau garde-fou import passe ; `RailOps lifecycle regression`, `RailOps modules regression` et `Final RLS hotfix check` sont en `success`, ainsi que le statut Vercel de branche. L’échec initial du lot v150B-2B provenait uniquement de deux anciens noms de fragments worklog invalides, corrigés ensuite sans changement runtime.
- Hygiène journal : anciens fragments d’inscription renommés au format contractuel et contrat durable ajusté pour reconnaître explicitement les fragments Git versionnés déjà référencés par `docs/worklog-railops.md`.
- `main` n’est pas modifié ni fusionné ; aucune activation RLS stricte et aucun changement des règles d’import métier au-delà de la tolérance déterministe décrite ci-dessus.
