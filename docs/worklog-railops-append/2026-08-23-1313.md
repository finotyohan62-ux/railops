## 2026-08-23 — commits `main` visibles dans le diagnostic de dérive 13:13 Europe/Paris

- État contrôlé avant modification : PR #1 toujours ouverte en brouillon sur `security/v150b2b-rls-ready`, non fusionnée ; comparaison fraîche avec `main` = **12 commits behind / 178 ahead** au départ. Supabase `railops` reste `ACTIVE_HEALTHY` sous PostgreSQL 17.6.1 ; Security Advisor relu en lecture seule, sans appliquer de changement.
- Amélioration réversible limitée à la CI de diagnostic : le résumé `Report branch drift (non-blocking)` affiche désormais jusqu’à 12 commits présents sur `main` mais absents de la branche (`git log --oneline --no-decorate HEAD..origin/main | head -n 12`), en complément de la liste de fichiers déjà ajoutée.
- Cycle rouge/vert confirmé par GitHub Actions : `371334dd9ac33b2370776c87a9fe70ce8a9a211a` (`test: require main-only commit summary`) fait échouer `v150B-2B checks` run #218 comme attendu ; `242f28f91be8dbef6a3762e5bf245d68cf7cbef7` (`ci: summarize main-only commits`) rend `v150B-2B checks` run #220 et `RailOps lifecycle regression` run #185 tous deux `success`. Vercel est également `success` sur le commit vert.
- État après modification : branche toujours divergente, **12 commits behind / 180 ahead** avant journalisation ; `main` reste intact et aucun merge/rebase n’a été tenté.
- Aucun code applicatif, règle métier, permission, migration, donnée Supabase, RLS, Import, Multi-chantier ou purge hebdomadaire n’a été modifié.
- Point en attente inchangé : la synchronisation avec les 12 commits de `main` et les smoke-tests humains restent soumis à validation explicite ; aucun nouveau choix produit n’a été introduit par cette passe.
