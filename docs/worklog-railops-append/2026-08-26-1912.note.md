## 2026-08-26 — clarification des comptages Supabase 19:12 Europe/Paris

- État contrôlé avant changement : branche `security/v150b2b-rls-ready` isolée de `main`; projet Supabase `railops` relu strictement en lecture seule et `ACTIVE_HEALTHY` sous PostgreSQL 17.6.1.084.
- Diagnostic : le dernier snapshot pouvait prêter à confusion entre `policy_count` (ex. `materiels=4`, `scans=4`, `users=2`) et le volume réel `row_count` observé (`materiels=1689`, `scans=121`, `users=34`). Aucune donnée n'a été modifiée.
- Amélioration documentaire réversible : `docs/supabase-state/README.md` impose désormais des libellés explicites `row_count`, `policy_count` et `rls_enabled`. Commit : `bdc44c80d25bb5bbcb01f476b1884b3f2e5f7f29` (`docs: clarify Supabase snapshot count semantics`).
- Vérification : document relu depuis la branche après commit ; statut Vercel `success`. Aucun code runtime, règle métier, permission, migration, donnée Supabase, RLS, Import, logique Multi-chantier ou purge hebdomadaire modifié ; `main` reste intact.
- Point en attente : aucun nouveau point nécessitant une décision utilisateur n'a été introduit ; les smoke-tests humains et toute évolution sécurité/données restent hors de cette passe.
