## 2026-08-26 — garde-fou CI non-mutant 01:14 Europe/Paris

- État contrôlé avant modification : branche `security/v150b2b-rls-ready` à `9dbda3570e09a7710d894f134e5c66b747d62bab`, comparaison fraîche avec `main` = **1 commit behind / 367 ahead** ; aucun merge/rebase/cherry-pick ni changement de `main`.
- Supabase relu strictement en lecture seule : PostgreSQL 17.6, RLS activé sur les huit tables cœur suivies et 16 policies au total ; aucune migration, permission, policy, donnée ou schéma modifié.
- Amélioration réversible limitée aux tests : `tests/v150b2b-ci-nonmutation.test.js` interdit désormais aussi dans la CI de vérification `git commit`, `git cherry-pick`, suppression de branche et bascule explicite vers `main`, en plus des push/merge/rebase/reset/déploiements déjà bloqués.
- Commit d’amélioration : `487d00da5ef5441b76618c54c43b63a7eb75c1f5` (`tests: harden CI nonmutation guard`).
- Vérification fraîche : GitHub Actions `v150B-2B checks` run #564, job `checks`, terminé `success`; le diagnostic de dérive et la suite agrégée sont tous deux verts.
- Garde-fous : aucun code runtime RailOps, règle métier, permission, RLS stricte, Import, logique Multi-chantier ou purge hebdomadaire modifié. Les points nécessitant validation humaine restent inchangés.
