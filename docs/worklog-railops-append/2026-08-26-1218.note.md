## 2026-08-26 — liste des fichiers de dérive runtime 12:18 Europe/Paris

- État contrôlé avant changement : `security/v150b2b-rls-ready` isolée de `main`; comparaison fraîche = **1 commit behind / 403 ahead** au départ, puis **1 behind / 405 ahead** après les deux commits de cette passe. `main` observé à `e89c57c995fe0661cffcdbfcf88b9f30a408a093`; Supabase `railops` est resté `ACTIVE_HEALTHY` sous PostgreSQL 17.6.1.084.
- Amélioration réversible limitée aux tests/diagnostics CI : le résumé de dérive affiche désormais la liste bornée des fichiers présents uniquement sur `main` ayant un impact runtime (application + `supabase/`), afin de cibler une revue de compatibilité sans fusionner ni synchroniser la branche.
- Cycle TDD vérifié : `ce66c8ad6b4ffa8b5c61888953750c112327e693` (`tests: require runtime-impact drift file list`) a fait échouer la suite comme attendu ; `5549a0413fa8442d528616dbe8242e7d14ae8663` (`ci: list runtime-impact branch drift files`) rend `Run v150B-2B verification suite` vert dans GitHub Actions run #638.
- Garde-fous : aucun code runtime RailOps, donnée Supabase, migration, policy, permission, RLS, règle métier, Import, Multi-chantier ou purge hebdomadaire modifié ; aucun merge, rebase, cherry-pick ni changement de `main` tenté.
- Point en attente inchangé : le commit runtime présent uniquement sur `main` reste volontairement non intégré et doit être revu avant toute future synchronisation.