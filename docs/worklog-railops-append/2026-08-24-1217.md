## 2026-08-24 — contrat des snapshots diagnostiques 12:17 Europe/Paris

- État contrôlé avant modification : PR #1 ouverte en brouillon sur `security/v150b2b-rls-ready`, non fusionnée ; comparaison fraîche avec `main` : **12 commits behind / 245 ahead** avant cette passe, merge-base `4b50df53ee449c4c907bae6a215672be3a2597d9`, `main` `283fca36c07f990ad80613d2aac97c2de3ef7bcf`. Aucun merge/rebase ni changement de `main`.
- Supabase vérifié strictement en lecture seule : projet `railops` `ACTIVE_HEALTHY`, PostgreSQL 17.6.1.084 ; les huit tables cœur suivies conservent RLS activé avec les mêmes nombres de policies (`agents` 0, `chantiers` 4, `deleted_ids` 1, `inspections` 0, `materiels` 4, `prix_catalogue` 1, `scans` 4, `users` 2). Security Advisor et Performance Advisor restent dans les catégories déjà connues.
- Amélioration réversible : ajout de `tests/v150b2b-snapshot-contract.test.js`, qui vérifie automatiquement que le snapshot diagnostique le plus récent conserve les sections GitHub/Supabase, les garde-fous lecture seule, la divergence avec `main` et l'identification metadata-only du projet.
- Commit : `980154bdeccd93d6eddb13e648774865b4c01032` (`tests: guard latest diagnostic snapshot contract`).
- Vérification fraîche : workflow `v150B-2B checks` run 352 **success** et workflow `RailOps lifecycle regression` run 252 **success** sur ce commit ; aucune modification backend n'a été effectuée.
- Garde-fous : aucun code applicatif, règle métier, permission, migration, donnée Supabase, RLS, Import, logique Multi-chantier ou purge hebdomadaire n'a été modifié.
- Points en attente inchangés : synchronisation avec les 12 commits de `main`, smoke-tests humains et toute correction sécurité/performance restent soumis à validation explicite ; aucun nouveau choix produit n'a été introduit.
